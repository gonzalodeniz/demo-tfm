# Recurso ConfigMap en Kubernetes llamado grafana-datasource dentro del namespace monitoring-elias-005.
# El ConfigMap contiene la configuración de la fuente de datos para Grafana en la clave datasource.yml.
# Esta configuración indica que Grafana debe conectarse a Prometheus
# (usando la URL http://prometheus.monitoring.svc:9090) como fuente de datos principal
# (isDefault: true) mediante acceso tipo proxy.

apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-configmap
  namespace: monitoring-elias-005
data:
  datasource.yml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: http://prometheus-service.monitoring-elias-005.svc:9090
        isDefault: true
